<?php require('template/header.phtml'); ?>

<div class="d-flex justify-content-center align-items-center h-100 custom-bg">
    <div class="dcustom-bg p-4 rounded" style="max-width: 400px;">
        <form method="post" action="Login.php">
            <div class="mb-3">
                <label for="username" class="form-label fw-bold text-light">Username</label>
                <input type="text" id="username" name="Username"
                    class="form-control bg-secondary bg-opacity-25 border-secondary text-white"
                    placeholder="Enter your username" required>
            </div>

            <div class="mb-3">
                <label for="password" class="form-label fw-bold text-light">Password</label>
                <input type="password" id="password" name="Password"
                    class="form-control bg-secondary bg-opacity-25 border-secondary text-white"
                    placeholder="Enter your password" required>
            </div>

            <!-- Display login errors if any -->
            <?php if (!empty($error)) { ?>
                <div class="alert alert-danger text-center"><?php echo htmlspecialchars($error); ?></div>
            <?php } ?>

            <div class="text-center mt-4">
                <button type="submit" class="custom-btn btn btn-primary">Login</button>
            </div>

            <div class="text-center mt-3 text-white">
                Don't have an account? 
                <a href="Register.php" class="custom-link">Register here</a>
            </div>
        </form>
    </div>
</div>
