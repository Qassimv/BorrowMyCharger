<?php require('template/header.phtml') ?>
<div class="container-fluid py-5 custom-bg">

    <div class="row justify-content-center">
        <div class="col-12 col-lg-10">
            <div class="mb-5">
                <div class="d-flex gap-2">
                    <input type="text" id="searchChargingPoints" class="form-control bg-dark text-white border-secondary"
                        placeholder="Search charging points...">
                    <select id="filterChargingPoints" class="form-select bg-dark text-white border-secondary">
                        <option value="all">All</option>
                        <?php if (!empty($view->chargerTypes)): ?>
                            <?php foreach($view->chargerTypes as $type): ?>
                                <option value="<?= htmlspecialchars(strtolower(str_replace(' ', '-', $type))) ?>">
                                    <?= htmlspecialchars($type) ?>
                                </option>
                            <?php endforeach; ?>
                        <?php endif; ?>
                    </select>
                </div>
            </div>

            <h2 class="text-white fw-bold fs-3 mb-3">All Charging Points</h2>

            <div class="row" id="chargingPointsContainer">
                <?php if (!empty($view->chargingPoints)): ?>
                    <?php foreach ($view->chargingPoints as $point): ?>
                        <div class="col-12 col-md-6 col-lg-3 mb-4 charging-point-card" 
                             data-type="<?= htmlspecialchars(strtolower(str_replace(' ', '-', $point['charger_type'] ?? ''))) ?>"
                             data-name="<?= htmlspecialchars($point['name'] ?? '') ?>"
                             data-address="<?= htmlspecialchars($point['address'] ?? '') ?>">
                            <div class="card bg-dark text-white">
                                <?php 
                                    $bgImage = 'images/default.jpg'; // fallback
                                    if (!empty($point['image_path'])) {
                                        $bgImage = htmlspecialchars($point['image_path']);
                                    } elseif (!empty($point['charger_type'])) {
                                        $type = strtolower($point['charger_type']);
                                        if ($type === 'dc fast charger') $bgImage = 'images/DC Fast Charger.jpg';
                                        elseif ($type === 'tesla charger') $bgImage = 'images/tesla charger.webp';
                                        elseif ($type === 'level 2 charger') $bgImage = 'images/Level 2 Charger.png';
                                    }
                                ?>
                                <div class="card-img-top bg-image" 
                                    style="background-image: url('<?= $bgImage ?>'); 
                                    background-size: cover; background-position: center; aspect-ratio: 16/9;">
                                </div>
                                <div class="card-body d-flex flex-column">
                                    <div>
                                        <h5 class="card-title"><?= htmlspecialchars($point['name'] ?? '') ?> - <?= htmlspecialchars($point['address'] ?? '') ?></h5>
                                        <p class="card-text text-secondary">Price: <?= htmlspecialchars($point['price_per_kwh'] ?? 'N/A') ?> BD per kWh</p>
                                        <p class="card-text text-secondary">
                                            Availability: 
                                            <?php if (!empty($point['available_from']) && !empty($point['available_to'])): ?>
                                                <?= htmlspecialchars($point['available_from']) ?> - <?= htmlspecialchars($point['available_to']) ?>
                                            <?php else: ?>
                                                24/7
                                            <?php endif; ?>
                                        </p>
                                        <?php if (!empty($point['username'])): ?>
                                            <p class="card-text text-secondary">Owner: <?= htmlspecialchars($point['username']) ?></p>
                                        <?php endif; ?>
                                    </div>
                                    <?php if (isset($_SESSION['role']) && $_SESSION['role'] === 'user'): ?>
                                        <a href="booking.php?id=<?= $point['charge_point_id'] ?>" class="btn custom-btn mt-4 w-100 fw-bold">Book Now</a>
                                    <?php endif; ?>
                                </div>
                            </div>
                        </div>
                    <?php endforeach; ?>
                <?php else: ?>
                    <div class="col-12 text-center text-white">
                        <p>No charging points available at the moment.</p>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<script>
// Client-side search and filtering
document.addEventListener('DOMContentLoaded', function() {
    const searchInput = document.getElementById('searchChargingPoints');
    const filterSelect = document.getElementById('filterChargingPoints');
    const chargingPoints = document.querySelectorAll('.charging-point-card');

    function filterChargingPoints() {
        const searchTerm = searchInput.value.toLowerCase();
        const filterType = filterSelect.value;

        chargingPoints.forEach(card => {
            const name = card.dataset.name.toLowerCase();
            const address = card.dataset.address.toLowerCase();
            const type = card.dataset.type;

            const matchesSearch = name.includes(searchTerm) || address.includes(searchTerm);
            const matchesType = filterType === 'all' || type.includes(filterType);

            card.style.display = matchesSearch && matchesType ? 'block' : 'none';
        });
    }

    searchInput.addEventListener('input', filterChargingPoints);
    filterSelect.addEventListener('change', filterChargingPoints);
});
</script>

<?php require('template/footer.phtml') ?>
